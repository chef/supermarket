<div id="contribute_sidebar" data-equalizer-watch>
  <h3>Chef's Open Source Projects</h3>

  <ul>
    <% Curry::Repository.all.each do |repository| %>
      <li><%= link_to repository.full_name, "https://github.com/#{repository.owner}/#{repository.name}", target: "_blank" %></li>
    <% end %>
  </ul>

  <% if current_user %>
    <h3>Your Agreements</h3>

    <% if current_user.contributors.empty? %>
      <p>You have not signed the Corporate Contributor License Agreement (CCLA) on behalf of a company yet. If you want to contribute to Chef's open source projects on behalf of your company, <%= link_to "ask an admin of your company to invite you", ccla_signatures_path %>. If your company has not yet signed the CCLA, <%= link_to "you can create a new CCLA signature", new_ccla_signature_path %>.</p>
    <% end %>

    <% unless current_user.signed_icla? %>
      <p>You have not signed the Individual Contributor License Agreement (ICLA) yet. If you are interested in contributing to Chef's open source projects as an individual, <%= link_to "you must sign the ICLA", new_icla_signature_path %>.</p>
    <% end %>

    <ul>
      <% if current_user.signed_icla? %>
        <li>
          <%= link_to "Signed ICLA on #{current_user.latest_icla_signature.signed_at.to_s(:longish)}", current_user.latest_icla_signature %>
        </li>
      <% end %>


      <% current_user.contributors.each do |contributor| %>
        <li>
          <%= link_to "Signed CCLA on behalf of #{contributor.organization.name} on #{contributor.organization.latest_ccla_signature.signed_at.to_s(:longish)}", contributor.organization.latest_ccla_signature %>
        </li>
      <% end %>
    </ul>
  <% end %>
</div>
