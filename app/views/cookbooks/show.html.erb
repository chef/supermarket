<%= provide(:title, "#{@cookbook.name} Cookbook") %>
<%= provide(:description, @cookbook.description) %>

<div class="cookbook_show">
  <%= render 'breadcrumbs', cookbook: @cookbook %>
  <%= render 'main', cookbook: @cookbook, version: @latest_version, cookbook_versions: @cookbook_versions %>
  <%= render 'sidebar', cookbook: @cookbook, version: @latest_version, supported_platforms: @supported_platforms, maintainer: @maintainer, collaborators: @collaborators %>
</div>
