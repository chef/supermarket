# This file was automatically generated by Habitat

# PostgreSQL Client Authentication Configuration File
# ===================================================
#
# Refer to the "Client Authentication" section in the PostgreSQL
# documentation for a complete description of this file.

# TYPE  DATABASE        USER            ADDRESS                 METHOD

###########
# Other authentication configurations taken from chef node defaults:
###########

# "local" is for Unix domain socket connections only
local all {{ cfg.process.user }} peer

host all {{ cfg.process.user }} 127.0.0.1/32 trust
host all {{ cfg.process.user }} ::1/128 trust
{{~ #each cfg.md5_auth_cidr_addresses as |cidr| }}
host all {{ ../cfg.process.user }} {{ cidr }} md5
{{~ /each }}

host    replication     {{cfg.replication.name}}     0.0.0.0/0               md5
{{ ~#each svc.members as |member| }}
host    replication     {{../cfg.replication.name}}     {{member.sys.ip}}/32           md5
{{ ~/each }}
